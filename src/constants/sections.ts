import type { SectionIdProps, SectionIdsProps } from '@/types/Section';

export const DEFAULT_SECTION: SectionIdProps = 'home';
export const SECTIONS: SectionIdsProps = ['home', 'about', 'experience'];
